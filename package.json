{
  "name": "ai-ticket-copilot",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "server"
  ],
  "scripts": {
    "dev": "npm run dev --workspace server",
    "build": "npm run build --workspace server"
  }
}
